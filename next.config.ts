import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  trailingSlash: true, // Optional: Adds a trailing slash to all routes (e.g., /about/ instead of /about)
};

export default nextConfig;
